This Job will start a Python based HTTP server

Required Arguments:
  port : port where the server will be listening

Optionnal Arguments:

Statistics: it does not produce any statistic

Example:
  - Start a server on the port 80 with the option : -a "port 80" 


---

- hosts: Agents
  vars_files:
    - vars/main.yml
  vars:
    - job_conf: traffic_monitor_http2_client.cfg
  
  tasks:
  - name: Create HTTP2 Client repository
    file: path=/opt/openbach-jobs/{{ job_name }} state=directory mode=0755
    become: yes

  - name: Install HTTP2 Client
    copy: src=files/{{ item }} dest=/opt/openbach-jobs/{{ job_name }}/ mode=0644
    with_items:
      - "{{ job_name }}.py"
      - "{{ job_name }}.help"
      - "{{ job_name }}_rstats_filter.conf"
    become: yes
    
  - name: Install HTTP2 Client configuration file
    copy: src=files/{{ job_conf }} dest=/opt/openbach-agent/jobs/ mode=0644
    become: yes
    
  - include: '{{ path_src }}/add_job.yml'


---

## Enable transmission of rsyslog to logstash for the job
- hosts: Agents
  vars_files:
    - vars/main.yml
  vars:
    - id: 1
    - job_options: restart
    - date_interval: date now

  tasks:
    - name: Enable Transmission
      shell: mv /etc/rsyslog.d/{{ job }}.conf.locked \
        /etc/rsyslog.d/{{ job }}.conf
      become: yes
      notify: Restart Rsyslog
      
    - include: ../../../roles/backend/tasks/start_job.yml


---

## Enable transmission of rsyslog to logstash for the application
- hosts: Agents
  remote_user: root
  vars_files:
    - vars/main.yml

  tasks:
   - name: Enable Transmission
     copy: src={{ filter_path }} dest={{ application_path }}
     notify: Reload Rstats


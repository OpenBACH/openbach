---

- hosts: Agents
  remote_user: root
  vars_files:
    - vars/main.yml
  vars:
    - job_conf: iperf.cfg

  tasks:
  - name: Inform Agent that Iperf is uninstalled
    file: path=/opt/openbach-agent/jobs/{{ job_conf }} state=absent

  - include: ../../../../roles/agents/tasks/remove_job.yml


This Job will start an HTTP2 server in order to generate traffic. It launchs the nghttpd command (without TLS).

Required Arguments:
  port : port where the server is available

Statistics: it doesn't produce any statistic

Example:
  - Start a server on the port 80: -a "port 80"


This Job will start an HTTP2 server in order to generate traffic. It only launchs the nghttpd command.

Required Arguments:
  port : port where the server is available

Optionnal Arguments:
  all the options of nghttpd are available

Statistics: it doesn't produce any statistic

Example:
  - Start a server on the port 80 with the option '--no-tls' : -a "port 80" "no_tls"


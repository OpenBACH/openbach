---

general:
  name:            iperf
  description: >
      This Job only launch the iperf command
  job_version:     0.1
  keywords:        [iperf]
  persistent:      True

os:
  linux:
    requirements:  'Ubuntu 14.04/16.04'
    command:       'iperf'
    command_stop:
  windows:
    requirements:  'Windows 2010'
    command:       '...'
    command_stop:

arguments:
  required:
    - name:        mode
      type:        'str'
      description: >
          To start the iperf in server mode, use '-s'
          To start the iperf in client mode, use '-c *server_ip*'
  optional:
    - name:        interval
      type:        'int'
      flag:        '-i'
      description: >
          Pause *interval* seconds between periodic bandwidth reports
    - name:        length
      type:        'int'
      flag:        '-l'
      description: >
          Set length read/write buffer to n (default 8 KB)
    - name:        port
      type:        'int'
      flag:        '-p'
      description: >
          Set server port to listen on/connect to to n (default 5001)
    - name:        udp
      type:        'None'
      flag:        '-u'
      description: >
          Use UDP rather than TCP

available_statistics:


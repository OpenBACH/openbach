---

- hosts: Agents
  vars_files:
    - vars/main.yml
  vars: 
    - id: 1
    - job_options: 172.20.0.83
    - date_interval: date now
  
  tasks:
    - include: ../../../../roles/backend/tasks/start_job.yml
    

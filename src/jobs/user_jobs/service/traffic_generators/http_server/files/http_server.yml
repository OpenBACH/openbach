---

general:
  name:            http_server
  description: >
      This Job will start an HTTP server in order to generate HTTP1 traffic.
  job_version:     0.1
  keywords:        [http, server]
  persistent:      True

os:
  linux:
    requirements:  'Ubuntu 14.04/16.04'
    command:       '/opt/openbach-jobs/http_server/http_server.py'
    command_stop:

arguments:
  required:
    - name:        port
      type:        'int'
      count:       1
      description: >
          Port where the server is available
   
  optional:

statistics:


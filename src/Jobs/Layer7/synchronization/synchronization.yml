---

- hosts: Agents
  remote_user: root
  vars_files:
    - vars/main.yml
  vars:
    - id: 1
    - job_options:
    - date_interval: date now

  tasks:
    - include: ../../../roles/agents/tasks/start_job.yml

